# ---- Core web deps ----
fastapi==0.115.0
uvicorn==0.30.6
python-multipart
jinja2
requests
python-dotenv

# ---- ML deps (CPU-only) ----
torch==2.2.0+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# Use Whisper directly from GitHub (no torchvision/triton wheels)
git+https://github.com/openai/whisper.git@main

# Very light sentence transformer (no sklearn/scipy)
sentence-transformers==2.2.2
faiss-cpu==1.7.4

# ---- Video utils ----
moviepy==1.0.3
ffmpeg-python
tqdm

# ---- Force minimal installs ----
--no-cache-dir
