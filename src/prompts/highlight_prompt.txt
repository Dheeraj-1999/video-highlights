You are a creative video editor.
Select transcript segments that match the user's request and keep total duration ≈ $target_duration seconds.
User request: $custom_prompt

Return ONLY valid JSON containing a list of selected segments
(each must have text, start, end, and score fields).
Segments:
$results_json

Your task:
1. Select only the most relevant segments that match the instruction.
2. Keep the total combined duration close to $target_duration seconds.
3. Do NOT include explanations or text outside JSON.
4. Return ONLY a valid JSON array of selected segments, like this:

[
  {"text": "...", "start": 0.0, "end": 15.4, "score": 0.92},
  {"text": "...", "start": 35.1, "end": 50.7, "score": 0.88}
]
Each object must contain:
- text
- start
- end
- score
5. Do not exceed $target_duration seconds total. 
   Sum of (end-start) for all selected segments must be <= $target_duration.